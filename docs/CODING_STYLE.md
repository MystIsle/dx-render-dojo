# 코딩 규약

기준 시점 : 2026-09-25

dx-render-dojo 의 C++ 코드 규약입니다. 코드를 쓰다가 걸리는 주제의 절만 찾아 읽으면 됩니다. 규약을 바꿀 때는 이 문서를 고칩니다.

이 문서에서 "원문"은 Rastertek DirectX 11 튜토리얼의 코드를 가리킵니다.

서식은 `.clang-format`, 네이밍은 `.clang-tidy` 로 검사합니다. 도구가 잡지 못하는 항목은 사람이 지킵니다.

## 1. 추상화

구조는 기법이 요구할 때 생깁니다. 미리 만드는 것은 도구와 뼈대뿐입니다.

- 현재 튜토리얼이 요구하지 않는 추상화는 만들지 않음. 중복은 3번째에서 뽑음
- 예외 : 아래 둘을 모두 만족하면 미리 넣음
  - 확실성 : 교재 진행이나 자체 렌더러가 요구함. "쓸 것 같다" 는 해당 없음
  - 비용 역전 : 나중에 넣는 비용이 지금보다 크게 높음. 태그 diff 가 그대로 교재 코드가 되므로 구조 변경은 뒤 편일수록 비쌈
- 예외로 미리 넣은 것 : 공통 도구(로그, 체크 매크로, 싱글톤 틀), 프레임 구조(`Update`/`Render` 분리), 실행 파일 기준 경로(`FPaths`)
- 예외가 아닌 것 : 백엔드 추상화(RHI), 렌더 그래프, 셰이더 공통 베이스. 요구되는 편에서 만듦

## 2. 소유

- COM 객체 : `ComPtr<T>`
- 내 객체 : 값 멤버가 기본. 다형성이 필요할 때, 다시 만들어야 할 때, 생성을 늦춰야 할 때만 `std::unique_ptr<T>`
- `shared_ptr` : 쓰지 않음
- raw 포인터·참조 : 빌리는 매개변수에만
- `new` / `delete` : 직접 호출 금지

해제는 값 멤버의 소멸자와 `ComPtr`·`std::unique_ptr` 가 맡습니다. 그래서 `Shutdown` 계열 함수와 `SafeRelease` 는 쓰지 않습니다.
창·디스플레이 설정처럼 스마트 포인터가 맡지 못하는 자원은 소멸자에서 돌려놓습니다.

**싱글톤 수명**

- `Get()` 은 인스턴스가 없으면 만듦. 대부분의 싱글톤은 이것만으로 충분함
- 생성 비용이 크거나 생성 시점을 잡아야 하면 `Prepare()` 로 앞당김. `FSystem` 이 여기에 해당함
- 해제 시점이 중요한 싱글톤만 `Release()` 를 부름. 그 밖은 프로세스 종료에 맡김
- `Shutdown` 계열 은퇴의 예외는 이 `Prepare`·`Release` 둘뿐임

## 3. 초기화

- 멤버 : 헤더에서 기본 초기화
- 생성자·소멸자 : 필요할 때만. 본문이 빈 생성자·소멸자는 쓰지 않음
- 복사 : `= delete`. 이때 기본 생성자가 사라지므로 `= default` 로 한 줄 적음. 베이스가 이미 막았으면 파생에는 적지 않음
- 실패를 호출자에게 넘기는 초기화만 `bool Initialize()`. 실패하면 그 자리에서 멈추는 초기화(`CHECK_FATAL`)는 `void Initialize()`
- `Initialize` 는 직접 일함. `InitializeShader` 같은 내부 위임 함수 금지

## 4. 에러

- 실패 검사는 두 매크로로 함
  - `CHECK_FATAL(expr)` : 계속할 수 없는 실패. 그 자리에서 로그 → 상자 → 종료 코드 1. 디버거가 붙어 있으면 그 줄에서 먼저 멈춤
  - `CHECK_RETURN(expr, false)` : 호출자에게 넘기는 실패. 로그를 남기고 반환
- 복구하는 실패(대체 경로가 있는 것)는 매크로로 검사하지 않고 `if` 로 직접 적음 (`if (SUCCEEDED(...))`). 매크로는 실패를 에러로 로그에 남김
- `if (FAILED(hr)) return false;` : 금지
- 두 매크로는 `HRESULT`·`bool`·포인터만 받음. 그 밖의 반환값은 성공 조건을 비교식으로 적음 (`RegisterClassExW(...) != 0`)
- `long` 을 돌려주는 함수(`LSTATUS` 를 돌려주는 `Reg*` 등)도 비교식으로 적음. `HRESULT` 가 `long` 이라 매크로가 받아 버리고, 양수 에러 코드를 성공으로 읽음
- `HRESULT` 가 실패하면 로그에 그 값이 함께 찍힘

`BOOL`·`ATOM` 을 받는 오버로드는 두지 않습니다. `int` 오버로드가 생기면 0 이 성공인 `errno_t`(`fopen_s` 등)의 판정이 뒤집히고, `short`·범위 없는 `enum` 을 넘겼을 때 나던 컴파일 에러도 사라집니다.

## 5. 문자열

- 경로 : `std::filesystem::path` 또는 `const wchar_t*`
- 보관 : `std::wstring`
- const 가 아닌 `char*` : 금지

## 6. 네이밍

언리얼 컨벤션을 전면 채택합니다.

| 대상 | 접두 |
|---|---|
| 클래스·구조체·공용체 | `F` |
| 인터페이스 (순수 가상 함수만 있는 클래스) | `I` |
| 추상 클래스 | `A` |
| 템플릿 | `T` |
| enum | `E` |
| bool 변수 | `b` |

- 멤버·로컬·매개변수·함수·파일 : 모두 PascalCase
- 열거자·네임스페이스·타입 별칭·템플릿 매개변수 : PascalCase. 타입 별칭과 템플릿 타입 매개변수는 가리키는 타입에 맞춰 `F`·`T`·`E` 를 붙여도 됨 (`template <typename T>`)
- 매크로 : 대문자와 밑줄 (`CHECK_FATAL`)
- 파일 이름 : 접두까지 클래스 이름과 같게 (`FModel` → `FModel.h`, `FModel.cpp`)
- 원문의 `Class` 접미사 : 떼고 접두를 붙임 (`ModelClass` → `FModel`)
- `On` 접두 : 남에게 등록되어 불리는 콜백 함수에만. 예 : `FSystem::OnResize` 는 `FWindow` 에 등록되는 콜백, `FWindow::HandleMessage` 는 `WndProc` 가 직접 부르는 함수

Rider 이름 규칙(`DxRenderDojo.sln.DotSettings`)은 이 절을 옮긴 것입니다. 바꿀 때는 이 절을 먼저 고치고, Rider 설정 › 에디터 › 코드 스타일 › C/C++ › 이름 지정 탭에서 맞춘 뒤 "솔루션 팀 공유" 로 저장합니다. 키가 GUID 라 파일을 손으로 고치지 않습니다.

`.clang-tidy` 가 잡지 못해 사람이 확인하는 것

- 파일 이름
- `T` 는 템플릿에만. 도구는 `T` 로 시작하는 클래스 이름을 검사하지 않음
- `I` 는 순수 가상 함수만 있는 클래스에만. 도구는 추상 클래스 중 `I` 로 시작하는 이름을 검사하지 않음
- bool 변수의 `b`. 도구는 `b` 로 시작하는 이름을 검사하지 않을 뿐, 그 변수가 bool 인지는 보지 않음
- `!` 대신 `== false` (8절). 도구가 검사하지 않음
- `On` 접두는 콜백에만. 도구가 검사하지 않음
- 선언 순서 (7절). 도구가 검사하지 않음

## 7. 헤더

- `#pragma once` 사용
- 매개변수 이름 : 항상 표기
- 값으로 받는 매개변수 : `const` 를 붙이지 않음. 호출하는 쪽에는 차이가 없음. 참조·포인터로 받을 때만 가리키는 대상에 `const` (`const FWindow& Window`)
- 한 줄 게터 : 인라인 `const`
- `using namespace` : 헤더에서 금지
- 전역 상수 : 두지 않음. 한 함수만 쓰면 그 함수 안의 `constexpr`, 여러 함수가 쓰면 클래스의 `static constexpr` 나 멤버로
- 클래스 안 상수 : `static constexpr`. `static` 없는 `const` 멤버 변수는 객체마다 값이 다를 때만
- include 경로 : `Source/` 기준으로 폴더까지 적음(`#include "Core/FSystem.h"`). 짝 헤더와 같은 폴더·하위 폴더의 헤더도 같음. 언리얼 엔진 코드와 에디터의 새 클래스 마법사가 모듈의 `Public` 루트 기준으로 적는 것과 같은 방식
- 접근자 : 읽는 쪽이 생길 때 만듦. 멤버를 더했다고 게터를 같이 만들지 않음. 예외는 클래스가 감싼 대상 자체를 꺼내는 접근자(`FWindow::GetHandle` 의 `HWND`). 래퍼는 그것을 내주려고 있으므로 읽는 쪽보다 먼저 둬도 됨. 그 밖에 쓰이지 않는 접근자는 죽은 코드
- 주석 : 지금 코드에 대해 계속 참인 문장으로 씀. 뒤 편의 계획이나 뒤 단계에서 생길 이름을 담지 않음. 구조가 바뀌면 그 편에서 주석도 고침

**선언 순서**

static 을 먼저, 함수를 변수보다 먼저 둡니다. `.cpp` 의 정의 순서도 헤더의 선언 순서를 따릅니다.

1. static 묶음 : 상수·함수·변수 각각 안에서 접근 구역은 public → protected → private
   - `static constexpr` 상수
   - static 함수 : 아래 함수 순서대로
   - static 변수
2. static 아닌 것 : 접근 구역은 public → protected → private
   - 구역 맨 앞 : 중첩 타입·별칭
   - 함수 : 생성·소멸 → 초기화 → 게터 → 세터 → 가상 → 일반 → 콜백
   - private 끝 : 변수. 선언 순서가 생성 순서라 의존하는 순서대로

- 생성·소멸 : 기본 생성자, 복사 `= delete`, 소멸자
- 초기화 : `Initialize` 와 수명 함수(`Prepare`·`Release`). 만든 직후 한 번 부르는 것만. 크기 변경처럼 일이 생길 때 다시 부르는 함수(`Resize`)는 일반 함수
- 콜백 : 남에게 등록해 불리게 하는 함수. 이름이 `On` 으로 시작함(6절). 콜백 안에서 직접 부르는 처리 함수(`HandleMessage`)는 일반 함수
- 함수가 `const`·`constexpr` 인지는 순서에 영향 없음. 하는 일로 나눔
- 빈 줄 : 묶음 사이에 하나. 같은 묶음 안의 빈 줄은 쓰는 사람이 정함

## 8. 서식

- 중괄호 : Allman
- 들여쓰기 : 탭
- 줄 길이 : 120자 상한
- 부정 : `== false` 로 적음. `!` 는 삼항 연산자 안처럼 줄 길이가 빠듯할 때만
- void 함수 끝 `return;` : 쓰지 않음
- 블록 뒤 빈 줄 2개 : 두지 않음
- 120자를 넘는 인자·매개변수 목록 : 한 줄에 하나씩
- 접근 지정자 : 들여쓰지 않음 (`public:` 을 `class` 와 같은 열에)
- 네임스페이스 : 안쪽을 들여씀 (`NamespaceIndentation: All`)
- 포인터·참조 : `*` 와 `&` 를 타입에 붙임 (`const wchar_t* Name`, `const FWindow& Window`)
- 그 밖의 서식 : Microsoft 스타일 기본값

120자 상한은 학습 노트 때문입니다. 노트의 코드 블록은 가로 스크롤 대신 자동 줄바꿈되므로 긴 줄은 읽기가 깨집니다.

## 9. 기각안과 변경 이력

**기각안**

- `m_camel` : 번거로움
- `_Upper` : 예약어
- `CHECK_RETURN` 의 `BOOL`·`ATOM` 오버로드 : 4절 참고

**언리얼식 네이밍 채택 (2026-09-13)**

2026-08-23 에는 "멤버/로컬 구분이 약함" 을 이유로 기각했습니다. 2026-09-13 에 아래 이유로 채택했고, 구분이 약한 문제는 감수합니다.

- 스터디원이 언리얼 트랙 출신이라 접두 어휘가 독자에게 익숙함
- 회사 UE5 코드와 표기가 같아져 손에 익은 대로 쓸 수 있음

**이 문서로 옮기면서 정한 것 (2026-09-13)**

- 파일 이름에 접두 포함
- 추상 클래스 접두 `A` 추가. 인터페이스 `I` 는 순수 가상 함수만 있는 클래스로 한정
- `Shutdown` 계열도 `SafeRelease` 와 함께 은퇴
- 원문 이름과 새 이름의 대응 표는 두지 않음. 클래스를 분리하면 원문과 대응되지 않는 경우가 있기 때문. 필요해지면 참고용으로 이 문서 위나 아래에 추가

**02편을 쓰면서 정한 것 (2026-09-13)**

- 생성자·소멸자는 필요할 때만. 창처럼 스마트 포인터가 맡지 못하는 자원은 소멸자에서 정리
- 전역 상수 금지. 쓰는 클래스의 `static constexpr` 나 멤버로
- `CHECK_RETURN` 은 `HRESULT`·`bool`·포인터만. 그 밖의 반환값은 비교식
- static 멤버는 클래스 맨 위
- 접근 지정자는 들여쓰지 않음

**02편 설계 수정 (2026-09-20)**

근거는 2026-09-20 조사 세션입니다. DirectXTK 템플릿·hw3d·learnd3d11·Godot·Unreal 을 대조했습니다.

- 소유는 값 멤버가 기본. 참고한 코드베이스가 모두 값 멤버를 기본으로 쓰고, `unique_ptr` 는 다형성·재생성·지연 생성일 때만 씁니다
- 1절 추상화에 예외 두 조건(확실성·비용 역전)을 넣었습니다. 태그 diff 가 그대로 교재 코드가 되어, 구조를 뒤 편에서 바꾸는 비용이 크기 때문입니다
- 3절 복사 금지에 "베이스가 이미 막았으면 파생에는 적지 않음" 을 넣었습니다. 베이스가 복사를 `= delete` 하면 파생의 복사는 컴파일러가 자동으로 delete 하고, 파생이 생성자를 선언하지 않았으므로 기본 생성자도 그대로 남습니다. `TSingleton` 을 상속하는 `FSystem`·`FLog` 가 여기에 해당합니다

**02편 코드 리뷰 (2026-09-20)**

- 2절에 싱글톤 수명 항목을 넣었습니다. 그 전에는 `wWinMain` 이 객체를 값으로 들고 `TSingleton` 은 포인터만 등록했는데, 싱글톤이라는 이름과 "밖에서 만들고 소유한다" 는 실체가 어긋났습니다. 이제 `TSingleton` 이 `static std::unique_ptr` 로 소유합니다
- 8절에 부정 표기를 넣었습니다. `!` 는 한 글자라 훑어 읽을 때 놓치기 쉽습니다
- 7절 전역 상수에 "한 함수만 쓰면 그 함수 안의 `constexpr`" 을 앞에 넣었습니다. 범위가 좁은 쪽을 먼저 적은 것이고 취지는 그대로입니다

**03편 설계 (2026-09-23)**

- 4절에 `CHECK_FATAL` 을 넣었습니다. 계속할 수 없는 실패는 발견한 자리에서 멈춥니다. 초기화 실패를 `wWinMain` 까지 올리던 구조에서는 상자에 가장 바깥 검사만 떴고, 초기화 실패 경로가 `FSystem::Release` 를 거치지 않았습니다
- 기각 : 첫 실패 이유를 기억했다가 `wWinMain` 에서 상자로 띄우는 안. 스택이 풀린 뒤라 디버깅에 불리하고, 이유를 나르는 장치가 따로 필요합니다
- 3절 `bool Initialize` 를 "실패를 호출자에게 넘기는 초기화만" 으로 좁혔습니다. `FWindow`·`FSystem`·`FD3D11Graphics` 의 `Initialize` 는 `void` 입니다
- 4절에 복구하는 실패는 `if` 로 적는다는 줄과 `long` 반환 함수 줄을 넣었습니다
- 치명 실패로 끝나기 직전에 D3D·DXGI 디버그 메시지를 로그로 내보내려고 `Return::SetFatalHook` 을 두었습니다. 원인을 설명하는 메시지는 실패 직전에 쌓이는데, 그 자리에서 끝나면 메시지를 비울 차례가 오지 않습니다

**04편 설계 (2026-09-24)**

- 1절 예외 목록에 `FPaths` 를 넣었습니다. 셰이더를 exe 옆에서 읽으려고 04편에 만들었고, 05편 텍스처도 같은 함수를 씁니다
- 공통 도구는 `Source/Utility/` 에 모읍니다. `Check`·`FLog`·`TSingleton` 은 02편부터 여기에 둡니다. 처음에는 04편에서 `Core` 에서 옮겼는데, 2026-09-25 이력을 다시 써서 02편부터 두었습니다

**선언 순서 (2026-09-25)**

- 7절에 선언 순서를 넣었습니다. 그 전에는 "static 멤버는 맨 위" 뿐이었고, 03·04편 코드는 게터를 public 맨 끝에 두는 모양으로 굳어 있었습니다
- 순서는 조은님이 전부터 쓰던 기조(생성·소멸 → 초기화 → 게터·세터 → 가상 → 일반 → 콜백)에 static 우선과 함수 → 변수를 더했습니다
- 6절에 `On` 접두를 콜백 전용으로 넣었습니다. 순서의 콜백 묶음을 이름으로 판정합니다
- 기각 : 게터를 public 맨 끝에 두는 모양. 둘 다 이치가 있어 오래 써 온 기조를 따릅니다
- 적용 : 이력 전체를 다시 써서 02편 첫 코드 커밋부터 새 순서로 맞췄습니다. 0단계 커밋까지만 해시가 그대로이고 `tut01` 부터는 모두 바뀌었습니다. 이 날 전에 적어 둔 `tut01` 과 그 뒤 커밋의 해시는 옛 이력을 가리킵니다

**Rider 이름 규칙 (2026-09-25)**

- Rider 기본 이름 규칙은 STL 방식(snake_case)이라 PascalCase 이름마다 점선 힌트가 떴습니다. 이 절을 옮긴 `DxRenderDojo.sln.DotSettings` 를 넣었습니다
- 옮기면서 6절에 없던 항목(열거자·네임스페이스·타입 별칭·템플릿 매개변수·공용체·매크로)을 더했습니다
- 같은 항목을 `.clang-tidy` 에도 더했습니다
- 설정 파일(DotSettings, `.editorconfig`·`.gitattributes` 줄, `.clang-tidy` 항목)은 이력을 다시 써서 `tut01` 커밋부터 넣었습니다. 태그에서 브랜치를 따도 Rider 설정이 따라갑니다
- 7절에 값 매개변수의 `const` 를 넣었습니다. Rider 가 값 매개변수마다 "상수로 만들 수 있다" 는 약한 경고를 띄웠습니다(02편 코드에서 30곳쯤). 붙이지 않기로 하고 DotSettings 에서 그 검사(`CppParameterMayBeConst`)를 껐습니다

**include·접근자·주석 (2026-09-25)**

- 7절에 include 경로를 옮겼습니다. 전에는 CLAUDE.md 에 "include 는 항상 폴더까지" 한 줄만 있었습니다. 짝 헤더만 파일 이름으로 적자는 안을 검토했고, 언리얼 엔진(`Private/Components/ActorComponent.cpp` 의 `#include "Components/ActorComponent.h"`)과 에디터 새 클래스 마법사(`GameProjectUtils` 가 옛 `#include "클래스.h"` 를 지우고 폴더를 포함한 경로로 바꿈)를 확인해 지금 규칙을 유지했습니다. UBT 는 `BuildSettingsVersion` V2 부터 `Public` 루트 기준입니다
- 기각 : 짝 헤더나 같은 폴더 헤더만 파일 이름으로. 한 헤더가 두 이름을 갖게 되고, 폴더를 옮기면 양쪽 모양을 다 고쳐야 합니다. 게임 템플릿처럼 하위 폴더를 모두 검색 경로에 넣으면 이름이 같은 파일이 부딪힙니다
- 7절에 접근자와 주석을 넣었습니다. 읽기 검토에서 02편 `FWindow::GetHandle`(03편에서 처음 씀)과 `FSystem::GetWindow`(쓰는 곳 없음), 02편 `FSystem.h` 의 "디바이스를 갖게 될 `FApplication`"(03편은 `FD3D11Graphics` 가 가짐)이 나왔습니다. 주석은 "창을 쓰는 멤버는 `FWindow` 뒤에 둔다" 로 바꿔 02편 5단계부터 이력에 반영했습니다. 03편 1단계에서 주석을 고치던 줄은 없어졌습니다
- 02편 `Main.cpp` 주석의 "창과 디바이스를" 도 같은 까닭으로 고쳤습니다(2026-09-26). 02편에는 디바이스가 없습니다. 같은 자리에 `FLog::Prepare()` 를 넣어 로그 콘솔을 창보다 먼저 엽니다. 늦게 열린 콘솔이 맨 앞에 와서 창을 누르기 전까지 키 입력을 가져갔습니다
- 접근자에 예외 하나를 두었습니다(2026-09-26). 필요할 때 만드는 것이 기본이지만 뻔히 있어야 할 것은 미리 둬도 된다는 조은님 의견을 따랐습니다. "뻔하다" 는 사람마다 달리 읽혀서 판별 기준을 "클래스가 감싼 대상 자체를 꺼내는가" 로 적었습니다. 창을 감싼 `FWindow` 의 `GetHandle` 은 여기에 들어 02편에 남겼습니다. `FSystem` 은 창을 감싼 클래스가 아니고 `GetWindow` 는 04편 끝까지 쓰는 곳이 없어서, 이력을 다시 써 02편 2단계부터 뺐습니다