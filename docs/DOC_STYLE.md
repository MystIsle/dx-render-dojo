# 교재 문서 규약

기준 시점 : 2026-09-25

dx-render-dojo 교재 문서의 편 구성과 문장 규칙입니다. 원고(`tools/doc-mock/template-NN.html`, NN 은 편 번호)를 쓰거나 고칠 때 읽습니다. 규칙을 바꿀 때는 이 문서를 고칩니다.

스테이지·편 제목·앞 편 태그는 목차 `tools/doc-mock/toc.json` 에 있습니다. 빌드가 원고 머리의 STAGE 줄과 이전 태그, 이전·다음 편 링크, 목차 페이지(`index.html`)를 여기서 채웁니다. 원고 `h1` 이 목차 제목과 다르면 빌드가 멈춥니다.

이 문서에서 "독자"는 DirectX 를 배우러 온 스터디원입니다. 리포를 관리하는 사람은 독자가 아닙니다. "원문"은 Rastertek DirectX 11 튜토리얼입니다.

셀 수 있는 규칙은 `tools/doc-mock/check_prose.py` 가 봅니다. 나머지는 8절의 목록으로 사람이 읽어 봅니다.

## 1. 원칙

규칙끼리 부딪치면 번호가 작은 쪽을 따릅니다.

1. **사실이 맞음.** 원고의 주장은 태그의 코드와 맞아야 하고, 한 단계의 주장이 다른 단계와 어긋나지 않아야 함
2. **따라 치면 단계마다 돌아가고, 끝나면 태그 코드가 됨.** 한 단계의 조각을 옮기면 그 단계 커밋의 코드가 되어 빌드·실행되고, 마지막 단계까지 옮기면 이 편의 태그 코드가 되어야 함. 조각의 조건은 2절
3. **독자에게 쓸모 있는 것만.** 리포 규약, 태그, 교재를 만든 사정, 작성자가 확인한 과정은 쓰지 않음. 이유를 적을 때는 독자에게 무엇이 달라지는지를 한 줄로. 예 : "왜 지금 만드나" 는 "03편부터 매 편에 쓴다" 로 충분
4. **빼도 잃는 것이 없으면 뺌.** 5절의 사족 네 종류가 대표
5. **요점 먼저.** 단계는 말할 것 하나를 첫 문단에, 문단은 요점을 첫 문장에
6. **나머지 문장 규칙(4절).** 문장 규칙을 지키려고 1~5를 어기지 않음

8절의 수치는 결과를 보는 지표입니다. 수치를 맞추려고 문장을 고치지 않습니다.

## 2. 편 구성

- **편마다 핵심을 먼저 정함.** 학습 블록은 핵심에만 붙이고, 곁가지는 언급만 한 뒤 설명을 접음. 모든 것에 블록을 붙이면 무엇이 중요한지 흐려짐. 예 : 02편의 핵심은 넷(창과 메시지 루프, 메시지가 객체까지 가는 길, 종료, 프레임 구조)이고 곁가지는 `TSingleton`·`FLog`·`CHECK_RETURN`·매니페스트
- **곁가지 단계는 본문 한두 문단.** 학습 블록을 붙이지 않고 줄 단위 풀이와 개념 상자를 접음. 칠 코드는 곁가지여도 펼침. 예 : 02편 1단계는 `FLog`·`TSingleton`·`Check.cpp` 코드를 펼치고 풀이와 개념 상자를 접음
- **단계 순서는 독자가 따라가는 흐름.** 파일 의존 순서(타입 → 도구 → 선언 → 구현)로 늘어놓지 않음. 새 타입의 선언은 처음 쓰이는 단계에 둠. 예 : 02편은 메시지가 흐르는 순서로 7단계
- **편은 단계 커밋 사슬로 씀.** 따라 하기의 단계 하나가 커밋 하나이고 N단계 커밋에 `tutNN-sN` 태그를 닮. 마지막 단계의 코드는 편 태그와 같음. 단계마다 빌드·실행되고, 단계 끝의 `여기까지` 상자에 그 단계에서 볼 결과를 적음. 실행할 것이 아직 없는 단계는 빌드만 확인함. 예 : 03편 1단계는 "로그에 그래픽 카드 이름", 02편 1단계는 빌드만
  - 중간 단계 코드는 최종 코드의 줄로만 씀. 뒤 단계에서 채울 값은 임시 값으로 두고 뒤 단계의 이름은 부르지 않음. 예 : 03편 1단계의 `CreateDXGIFactory2(0, ...)` 는 5단계에서 `FactoryFlags` 가 됨. 중간 단계만을 위한 주석은 만들지 않음
  - 한 함수에 여러 단계의 일이 섞이면 단계마다 그 몫만 더함. 인자도 쓰는 단계에서 늘림. 예 : 03편 `FD3D11Graphics::Initialize` 는 1단계에 인자 없이, 2단계에 `Window`, 3단계에 `Settings`
  - 예외 : 뒤 단계가 함수를 통째로 바꾸고 그 이유가 교재 내용이면 앞 단계는 옛 모양으로 둠. 예 : 02편 2단계의 `FWindow::Initialize` 는 바깥 크기를 먼저 계산해 창을 만들고, 7단계가 배율을 읽으려고 창부터 만드는 모양으로 바꿈
  - 마커의 `@N` 이 N단계 커밋을 읽음. `DIFF@N` 은 N-1단계에서 N단계로 온 차이. `@` 가 없는 마커는 편 태그를 읽고 바뀐 파일 절에 씀
- **코드 조각은 따라 칠 수 있게.** 단계 순서는 흐름 그대로 두고 조각은 아래를 지킴
  - 빠짐없음 : N단계 diff 에서 바뀐 줄은 지운 줄까지 모두 N단계의 어느 조각에 나옴. 공백만 바뀐 줄과 끝의 바뀐 파일 절은 세지 않음. 요점 밖의 코드는 접기에 둠
  - 보이는 방식 : 새 함수·클래스와 바뀐 함수는 통째로. 흩어진 한두 줄과 지운 줄은 범위 diff 로. 절반 넘게 바뀐 파일은 파일째
  - 새 파일 : 그 단계가 쓰는 include 와 멤버만 둠. 뒤 단계가 쓰는 것은 그 단계에서 diff 로 더함
  - 덩어리 순서 : 한 단계 안에서 한 덩어리(함수 본문, 클래스 선언, include 묶음)를 여러 조각으로 나눠 보이면 조각들이 빈틈없이 이어지고, 앞 조각이 소스에서도 앞
  - 넣을 자리 : 캡션에 배지 `신규`·`수정`·`삭제` 중 하나. 함수 중간 조각은 캡션이나 앞 문장에 자리를 적음. 예 : "`FD3D11Graphics::Initialize` 안, `CreateSwapChainForHwnd` 다음". 새 함수도 캡션에 자리를 적되, 그 단계까지 친 함수를 기준으로 적음. 예 : "`Initialize` 다음, `CreateSizeDependentResources` 앞"
  - 들여쓰기 : 조각은 소스의 들여쓰기 그대로
  - 한 단계 안에서는 선언을 쓰는 코드보다 먼저. 구조체·헤더를 cpp 보다 먼저 치고, 새 클래스를 만든 뒤 그 클래스를 부르는 쪽을 고침. 예 : 03편 3단계는 `FDisplaySettings::bVSync` → `FD3D11Graphics.h` → `Initialize` 순서
  - 프로젝트 파일 : 새 파일이 생기는 단계에서 등록함. `.vcxproj`·`.vcxproj.filters` 는 문장으로 안내(솔루션 탐색기의 기존 항목 추가). 파일 추가로 생기지 않는 속성 변경은 속성 경로까지 적음. 예 : 링커 › 입력 › 추가 종속성. `CMakeLists.txt` 는 `CMake · 맥 빌드와 검사용, VS 만 쓰면 건너뜀` 접기에 조각으로 둠. CMake 가 `Source/` 아래 `.cpp` 를 모으므로 CMake 조각은 포함 경로·링크 라이브러리처럼 파일 추가 밖의 설정이 바뀌는 단계에만 나옴
  - 옮긴 파일 : 어디서 어디로 옮기는지 문장으로 안내함. 파일 탐색기로 옮기고, 솔루션 탐색기에서 옛 항목을 제거한 뒤 기존 항목 추가로 다시 넣음. 조각은 바뀐 줄만 범위 diff 로 보임. 예 : 04편의 `Core` → `Utility`
- **새 이름은 코드나 개념 상자가 보여 준 뒤에 부름.** 앞에서 불러야 하면 "뒤에 나올 `FSystem`" 처럼 예고함. "한눈에 보기" 의 클래스 목록이 이 역할을 함
- **블록.** 먼저 예측 2문항, 자기 설명 질문(핵심 단계에만), 확인 문제 3문항(전부 핵심에서), 바꿔 보기(핵심을 건드리는 과제만), 다음 편 미리 생각하기 한 줄. 바뀐 파일은 접어 둠. 따라 하기 → 다 됐는지 확인 → 흔한 실수 순서

## 3. 단계와 문단

- **단계마다 말할 것 하나.** 그 요점에 걸리는 코드에만 본문을 쓰고, 나머지 코드의 설명은 그 코드 아래 줄 단위 풀이로 내림. 산문에 대한 규칙이고, 코드는 2절대로 빠짐없이 보임. "코드 → 한두 문장" 이 세 번 넘게 이어지면 설명서로 읽힘. 예 : 02편 2단계의 요점은 "800×600 은 클라이언트 영역 크기" 하나이고 `FDisplaySettings`·`FWindow` 선언 설명은 풀이가 맡음
- **문단은 요점으로 엶.** 첫 문장만 읽어도 그 문단이 무슨 말인지 알아야 함. 예 : "`Initialize` 가운데의 크기 계산은 800×600 이 클라이언트 영역의 크기라서 필요해요" 는 요점이 이유 절 안에 묻힘. "800×600 은 창 전체가 아니라 클라이언트 영역의 크기예요" 로 엶
- **단계 첫 문단도 요점이나 문제로 엶.** 앞 단계와 이어야 하면 한 구절로 잇고 회상 문장을 따로 두지 않음. 모든 단계가 "앞 단계 회상 → 빈 곳 지적" 으로 열리면 그것이 틀로 보임. 예 : 2단계는 "Win32 에서 창을 띄우려면 호출이 두 번 필요해요" 로 바로 시작
- **소제목(h3)은 2~5어절 동사구.** 같은 목적이 다음 편에 또 나오면 같은 라벨을 다시 씀. 목적이 같은 단계를 편끼리 알아보게 하는 장치(subgoal labeling)이고, 문체 리서치에서 프로그래밍 교육 실험 근거가 붙은 유일한 항목. 02편 라벨 : `공통 도구 셋을 미리 만들어 둔다` · `창 클래스를 등록하고 창을 만든다` · `static 함수에서 객체를 찾는다` · `메시지를 맡을 곳으로 나눠 보낸다` · `메시지 루프를 돌린다` · `끝나는 길을 하나로 모은다` · `진입점을 쓰고 빌드에 등록한다`
- **문단을 요약 꼬리나 예고로 닫지 않음.** "N단계에서 봐요" 는 풀이 항목 끝이나 답 보기의 괄호 링크로 돌림. 코드에 이미 나온 이름에는 예고를 달지 않음
- **개수 선언으로 문단을 열지 않음.** "끝나는 길은 셋이에요" 대신 셋을 바로 나열함. 독자가 세어 확인할 값("새 `.cpp` 여섯 개")은 남김

## 4. 문장

- **어미.** 본문은 해요체. 불릿·레이블·표는 명사 종결, 제목은 동사구. 푸터의 라이선스 문구만 합쇼체
- **술어는 직설로.** 직설로 바꿔도 어색하지 않으면 바꿈(`깔아 둡니다` → `미리 만들어 둡니다`, `닿을 수 있게` → `부를 수 있게`). 이름으로 쓰는 비유(`곁가지`, `번호표`)와 프로그래밍 표준어(`넘기다`·`돌려주다`·`부르다`·`오다`)는 둠
- **코드 위치는 이름으로 가리킴.** "가운데", "맨 위", "아래 코드" 대신 함수·`case`·변수 이름을 씀. 같은 함수 안에 그 말이 다른 뜻으로 쓰일 자리가 있으면 잘못 읽힘. 예 : `FWindow::Initialize` 는 창을 화면 가운데에 놓는 계산도 해서 "가운데의 크기 계산" 이 그쪽으로 읽힘
- **두 문장은 인과나 대조일 때만 이음.** 상관없는 두 사실을 `-는데`·`-고` 로 잇지 않고 끊음. 예 : "`FInput` 은 … 기록하고, `FSystem` 은 창과 입력을 멤버로 가져요" 는 두 사실이라 나눔
- **쉼표는 주어가 바뀌는 긴 문장에.** 연결어미 뒤 쉼표는 기본으로 빼되, 주어가 바뀌어 한 번에 읽히지 않으면 남김. 예 : "창을 파괴하는 코드는 소멸자에만 두고, 끝내자는 신호는 `PostQuitMessage` 로만 보내요"
- **"~는 것은 ~예요" 정의문을 몰아 쓰지 않음.** 한 단계에 셋이 모이면 번역한 문장처럼 읽힘. 예 : v2 4단계의 "이 함수가 정하는 것은 …", "기준은 …", "등록하는 곳은 …"
- **지우지 않는 표현.** `~것이다`·`~를 통해`·문두 `또한`/`따라서` 는 사람이 더 많이 쓰는 표현이라 AI 티로 보고 지우지 않음
- **줄표(—) 대신 콜론이나 마침표**

## 5. 사족

빼도 독자가 잃는 정보가 없는 문장입니다. 02편은 v2 에서 v3 로 고치며 따라 하기 본문이 98문장에서 73문장으로 줄었고, 뺀 문장 대부분이 아래 넷이었습니다.

- **코드를 다시 읽어 주는 문장.** 바로 옆 코드나 코드 제목 줄이 보여 주는 것을 산문으로 옮긴 것. 예 : "`Update` 는 `FSystem::Get().GetInput()` 으로 입력을 읽고 ESC 가 눌려 있으면 `RequestExit` 을 불러요"
- **같은 설명의 반복.** 코드 주석, 개념 상자, 앞 단계, 예측 답이 이미 말한 것. 같은 어휘로 두 번 나오면 한 손이 쓴 티도 남음. 예 : "창마다 앱이 마음대로 쓰는 포인터 크기의 칸" 이 3단계 본문과 바로 아래 개념 상자에 둘 다 있었음
- **작성자가 확인한 과정.** 독자가 쓰는 것은 결과. 6절 참고. 예 : "컴퓨터를 실제로 꺼 가며 확인하기 어려워서 이 메시지를 창에 직접 보내 봤어요"
- **지금 몰라도 되는 예고.** 뒤 편의 계획이나 아직 쓰이지 않는 코드의 현재 상태. 예 : "시간을 다루는 편에서는 `Update` 가 경과 시간을 받게 돼요", "`OnResize` 는 아직 로그만 남겨요"

허용하는 반복은 하나입니다. 같은 사실을 본문과 흔한 실수에 둘 다 둘 수 있습니다. 본문은 "왜", 흔한 실수는 증상과 해결을 맡고, 흔한 실수 항목 아래 `본문` 레이블 줄로 본문을 가리킵니다.

## 6. 실측과 숫자

- **독자가 자기 화면에서 볼 결과만 씀.** 확인한 방법과 순서는 쓰지 않고, 결과는 현재형으로 씀. 예 : "확인해 보니 `WM_GETMINMAXINFO` 가 먼저 왔고" → "`WM_GETMINMAXINFO` 가 먼저 와요"
- **결과를 확인한 방식보다 세게 쓰지 않음.** 디버거로 메시지를 보내 본 결과를 "끌어 옮겨 봤다" 로 쓰지 않음. 손으로 확인하지 않은 값은 원고에 넣기 전에 확인함
- **계산값은 계산값으로.** 측정하지 않은 값에 "였어요" 를 쓰지 않음. 환경 조건(배율 100%)을 붙이고, 조건이 다른 독자에게는 값이 달라질 수 있다고 알림. 예 : "배율 100% 에서는 … 바깥 크기가 816×639 가 돼요"
- **1인칭은 원문과 결정이 갈린 자리에만.** "저는 반대로 정했어요" 처럼 판단을 밝힐 때 한 편에 한두 번. 막다른 길을 보여 주는 서술("`static` 을 지우고 빌드하면 … 에러가 나요")은 문제가 저절로 있는 자리에 한 번. 그 밖은 설명을 줄이고 이유는 접기로
- **실측 숫자는 과제로도 씀.** 본문이나 확인 목록에 한 번, 바꿔 보기에 한 번. 독자가 자기 화면에서 같은 숫자를 보면 과제의 출발점이 됨. 예 : 최소 크기의 로그 `304x201` 이 바꿔 보기 3번의 출발점
- **출처가 계획인 문장은 계획을 따라 고침.** 예 : 6단계의 "정리가 끝나기 전에 프로세스가 끝날 수 있어요" 는 볼트 계획 노트의 "고치지 않기로 한 것" 이 출처

## 7. 학습 블록과 접기

- **접기 라벨 다섯은 이름을 고정함.** `Win32 개념` · `D3D 개념` · `줄 단위 풀이` · `원문과 비교` · `언리얼로 치면`. `D3D 개념` 은 Direct3D 와 DXGI 의 객체·용어를 담음. 내용이 있는 단계에만 둠. 모든 단계에 같은 접기가 붙으면 그것이 틀로 보임
- **접기는 읽을거리에만.** 칠 코드는 접지 않음. 접기 안의 코드는 읽을거리로 보여 건너뛰게 됨. 예외는 VS 조작이 남기는 프로젝트 파일 diff(`참고 : …`)와 CMake 조각으로, 둘 다 VS 로 따라 하는 독자는 치지 않음
- **줄 단위 풀이는 그 코드 바로 아래.** 단계 끝으로 모으지 않음
- **개념 상자는 그 단계를 읽는 데 필요할 때만 펼쳐 둠.** 곁가지 단계의 개념 상자는 접음
- **자기 설명 질문이 묻는 것을 같은 단계 본문이 말하지 않음.** 겹치면 둘 중 하나를 뺌. 예 : 4단계 본문에서 키 입력이 거치는 함수 나열을 빼고 F1 추적 질문만 남김
- **흔한 실수의 증상은 이 교재 코드를 따라 한 독자가 실제로 볼 수 있는 것.** 증상이 나지 않는 항목은 본문으로 옮기거나 조건을 증상 줄에 적음. 예 : 02편 `HandleMessage` 는 `WM_GETMINMAXINFO` 에서 멤버를 읽지 않아, `WndProc` 의 `nullptr` 검사가 없어도 "크래시" 증상이 나지 않음
- **언리얼 상자는 엔진 소스로 확인한 심볼만.** 확인 목록은 볼트 `_ai/2026-09-20_튜토리얼-학습-방식-검토기록` 에 있음. 목록에 없으면 상자를 빼거나 엔진 소스에서 먼저 확인

## 8. 점검

`check_prose.py` 가 보는 것 : 마커와 태그 코드의 대응, 따라 하기 조각의 단계별 빠짐없음과 덩어리 순서, 단계와 맞지 않는 `@N` 마커, 마지막 단계 커밋과 편 태그의 코드 일치, 옮긴 파일의 언급과 바뀐 줄, 태그 짝과 앵커, 줄표, 본문 합쇼체, 금지 표현(메타 담화·강조 예고·술어 비유 등), 개수 선언으로 여는 문단, 예고 꼬리, 제목과 목록의 어미.

```
./tools/doc-mock/build.ps1 -Lesson 2
python tools/doc-mock/check_prose.py tools/doc-mock/template-02.html --tag tut02
python tools/doc-mock/check_prose.py tools/doc-mock/tutorial-02.html --built
```

`build.ps1` 을 인자 없이 돌리면 원고가 있는 편을 모두 다시 만듭니다. 새 편을 쓰면 앞 편의 다음 편 링크가 바뀌므로 인자 없이 돌립니다. 목차 페이지는 어느 쪽이든 다시 만듭니다.

사람이 보는 것 :

- 원고의 사실 주장을 태그의 코드와 대조. 특히 "항상", "몇 개든" 같은 전칭 주장
- 한 단계의 주장이 다른 단계의 코드·서술과 맞는지. 예 : v2 6단계의 "소멸자는 … 저절로 불리니 길이 몇 개든 따로 챙길 것이 없어요" 는 7단계가 `Release` 를 직접 부르고, 초기화 실패 경로는 그것도 거치지 않아 어긋났음
- 문단마다 요점이 첫 문장에 있는지
- 5절의 사족
- 스크립트가 "문단 끝의 짧은 문장" 으로 보고한 줄이 요약 꼬리인지
- 편마다 한 번 인수 시험 : 이전 태그를 빈 worktree 에 풀고 원고 조각만 보고 단계마다 옮겨 빌드·실행한 뒤, 그 단계 커밋과 diff 가 비는지 봄. 공백과 빈 줄은 무시하고, 프로젝트 파일은 빌드와 실행 결과로 판정
- 단계 커밋은 커밋마다 빌드하고 실행해 `여기까지` 상자의 결과가 나오는지 봄

원고를 고친 뒤에는 `python tools/doc-mock/compare.py tools/doc-mock/template-NN.html` 로 마지막 커밋과 나란히 비교해 봅니다. 뺀 문장과 새로 쓴 문장이 절별로 표시됩니다. `--rev` 로 다른 리비전, `--old` 로 다른 파일과도 비교합니다.

스크립트는 쉼표가 든 문장 비율과 100자 이상 문장 수를 사람·AI 참고치(쉼표 26%·61%, 1000문장당 91·8)와 함께 보고합니다. 방향만 봅니다. 비율을 낮추려고 쉼표를 빼다가 긴 문장이 한 번에 읽히지 않게 되면 되돌립니다.

## 9. 기각안과 변경 이력

**산문 기조 결정 (2026-09-20)**

볼트 `_ai/2026-09-20_한국어-학습문서-문체-리서치` 를 읽고 정했습니다. 위화감의 원인은 번역체보다 구조와 리듬이었습니다. 여덟 단계가 같은 슬롯으로 찍혀 나오고, "왜 필요한가" 가 여덟 번 같은 질문을 하며, 문단마다 짧은 요약으로 닫히는 것이 AI 티였습니다.

- "왜 필요한가" 슬롯은 없애고 첫 문단에 녹임
- 간결판/상세판 토글은 두지 않음. 간결판에 손으로 적은 코드 조각이 생겼음
- h3 아래 "한 줄 what" 슬롯은 두지 않음

**02편 원고 v3 검토 (2026-09-23)**

v2 를 태그 코드와 대조한 검토에서 바꿨습니다. v2 는 점검 오류가 0이었는데도 따라 하기 본문을 4분의 1 줄일 만큼 사족이 있었고, 두 단계의 주장이 서로 어긋났습니다.

- 실측 자리에 확인 과정을 섞던 규칙을 결과만 쓰는 것으로 바꿈. 확인 과정 서술이 사족이 됐음
- "단계 첫 문단은 앞 단계가 남긴 것에서 출발" 을 "요점이나 문제로 엶" 으로 바꿈. 회상 문장이 요점을 뒤로 밀었음 (3단계는 요점이 셋째 문단에 나왔음)
- "인접 두 문장을 이어 긴 문장을 문단마다 하나 둠" 을 "인과·대조일 때만 이음" 으로 바꿈. 상관없는 사실을 `-는데` 로 잇는 문장이 생겼음
- 쉼표 비율을 목표에서 지표로 내림
- 새로 넣음 : 1절의 우선순위, 사실과 단계 간 일관성, 사족 네 종류, 요점 먼저, 코드 위치는 이름으로, 계산값 표기, 곁가지 분량, 흔한 실수의 증상 조건
- 규칙은 볼트 노트 "Rastertek 재구축 계획" 의 산문 기조 절에 있었고 이 문서로 옮김

**03편 원고 구성 (2026-09-23)**

- 접기 라벨에 `D3D 개념` 을 더함. 디바이스·스왑체인 같은 개념은 `Win32 개념` 에 맞지 않았음

**따라 치기 기본 (2026-09-24)**

03편 v1 은 단계 조각만 옮겨서는 빌드되지 않았습니다. 추가된 537줄 가운데 124줄, 지운 70줄 가운데 22줄이 바뀐 파일 절에만 있었고, 한 함수를 나눈 조각의 순서도 소스와 달랐습니다. 02편은 722줄 가운데 286줄이었습니다.

- 1절 원칙 2번에 "따라 치면 태그 코드가 됨" 을 넣음. 사족을 빼는 규칙보다 앞
- 2절에 코드 조각의 조건. 단계 순서는 흐름 그대로
- 3절의 "요점에 걸리는 코드" 는 산문 규칙으로 좁힘
- 새 파일은 나눠서 침. 기각 : 처음 나오는 단계에서 통째로. 빠짐없음은 저절로 되지만 설명보다 칠 코드가 먼저 오고 뒤 단계 조각과 겹침
- 기각 : 파일 순서(헤더 → cpp)로 단계 재배치. 따라 치기에 필요한 것은 파일 순서가 아니라 빠짐없음·넣을 자리·덩어리 순서였음
- 보류 : 단계마다 빌드. 코드가 강의 순서로 짜이지 않아 단계별 커밋을 만들려면 끝난 코드를 쪼개 되쌓아야 함. 스터디에서 중간 확인이 필요해지면 다시 봄
- 적용 : 03편부터. 02편은 03편으로 규칙과 도구를 검증한 뒤 고침

**03편 인수 시험 (2026-09-24)**

코드 세션이 03편 v2 의 따라 하기 조각만 옮겨 빌드와 실행까지 확인했습니다. 빠진 줄은 없었고, `tut03` 과의 diff 는 새 함수 순서와 vcxproj 추가 종속성 위치 두 곳이 남았습니다.

- 새 함수의 넣을 자리를 캡션에 적게 함. 새 파일 안의 새 함수 여섯 개에 안내가 없어 파일 끝에 붙였고 순서가 달라졌음
- 조각의 들여쓰기를 소스 그대로 둠. SLICE 가 공통 들여쓰기를 벗겨 함수 안 조각이 0열로 나오고, `#ifndef` 가 든 조각은 안 벗겨져 들쭉날쭉했음
- 인수 시험은 공백과 빈 줄을 무시하고, 프로젝트 파일은 빌드와 실행 결과로 판정. VS 가 "모든 구성" 의 추가 종속성을 어디에 적는지는 확인하지 못했음

**04편 옮긴 파일 (2026-09-24)**

04편에서 공통 도구 다섯 파일을 `Core` 에서 `Utility` 로 옮겼습니다. 점검 도구가 새 경로 하나로만 diff 해서 옮긴 파일을 새 파일로 셌습니다. 그래서 바뀐 include 11줄 대신 284줄을 조각으로 요구했습니다.

- 2절에 옮긴 파일 항목을 넣음
- `check_prose.py`·`build.ps1` 은 옛 경로와 짝지어 diff 함
- 2026-09-25 이력을 다시 써서 공통 도구를 02편부터 `Utility` 에 둠. 이 옮기기는 지금 이력에 없고, 규칙과 도구는 다음에 파일을 옮길 때 씀

**배지 이름 (2026-09-25)**

- 코드 조각 배지를 `새로`·`바꿈`·`지움` 에서 `신규`·`수정`·`삭제` 로 바꿈. `새로` 는 부사라 이름표로 어색했음
- `바꿈` 은 통째로 바꿔 넣는 조각과 diff 조각에 함께 붙어서 "교체" 가 아니라 "수정" 으로 함

**목차 (2026-09-25)**

- STAGE 구분은 옛 사이트(dxstudy-rastertek.vercel.app) 목차의 14개를 따름. 원문에 없는 편은 스테이지 밖 "원문에 없는 편" 묶음에 둠
- 스테이지와 편의 대응, 편 제목, 앞 편 태그를 `toc.json` 한 곳에 둠. 앞 편 태그를 번호에서 1을 빼 구하던 것을 바꿈. `tutalpha01` 의 앞 편은 `tut30` 이라 번호로 알 수 없음
- 이전·다음 편은 목차 순서. 원고가 아직 없는 편은 링크 없이 흐리게 둠
- 편 한 줄 요약은 원고 머리 요약의 첫 문장. 해요체 문장이라 목차 줄(`<li class="entry">`)은 목록 항목의 명사 종결 점검에서 뺌
- 기각 : 카드 모양 목차. 원고가 없는 편이 빈 카드로 늘어서 자리를 많이 차지했음
- 보류 : 원고마다 JS 에 박힌 진도 막대(61편, 공통 범위)는 목차에서 채우지 않음. 공통 범위가 잘 바뀌지 않음

**단계 스냅샷 (2026-09-25)**

학습자 레포에서 03편을 따라 치다 2단계에서 막혔습니다. 조각이 태그의 최종 코드를 잘라 와서 2단계 조각이 4·6·7단계에서 생기는 `bVSync`·`GetDpi`·`FlushDebugMessages` 를 불렀고, 03편은 7단계 끝까지 한 번도 빌드되지 않았습니다. 접기 안의 `Check.cpp` 는 읽을거리로 보여 건너뛰었습니다. 2026-09-24 에 보류한 "단계마다 빌드" 를 다시 볼 조건이 생겨 채택했습니다.

- 1절 원칙 2번과 2절에 단계 커밋 사슬을 넣음. 태그는 `tutNN-sN`
- 2절에서 뺀 것 : "새 파일은 include 묶음과 클래스 선언을 처음 나오는 단계에서", "코드는 앞 단계, 설명은 뒤 단계". 둘 다 뒤 단계 이름을 앞 단계로 당겨 왔음
- 한 단계 안에서 선언을 쓰는 코드보다 먼저 둠. 구조체 필드를 먼저 더하고 그 필드를 쓰는 함수를 뒤에 침
- 7절에 칠 코드 접기(`fold must`)를 넣음
- 03편을 7단계 커밋으로 다시 쌓음. "빌드에 등록한다" 단계는 없애고 2단계로 합침. `MakeWindowAssociation` 은 Alt+Enter 를 처리하는 6단계로, 치명 실패 훅은 디버그 메시지를 다루는 7단계로 옮김
- 03·04편은 main 이력을 다시 쓰지 않음. 앞 편 태그에서 가지를 쳐 단계 커밋을 쌓고 마지막 커밋의 코드가 편 태그와 같은지만 봄. 05편부터는 작업 브랜치에서 처음부터 단계 순서로 커밋함
- 기각 : 지금 원고에 "`bVSync` 는 4단계에서 추가" 같은 표시만 다는 안. 7단계까지 빌드되지 않는 것은 그대로임
- 기각 : 커밋 없이 조각 배치만 바꾸는 안. 한 함수 가운데 섞인 뒤 단계 줄(03편 `FSystem::Initialize` 의 배율 로그)은 최종 코드에서 뺄 수 없음

**창 코드를 02편으로 (2026-09-25)**

03편 7단계 가운데 실패 처리(1단계)와 배율·전체 화면(6단계)은 D3D 와 상관없는 창 코드였습니다. 03편을 쓰다 결정을 바꾸며 들어온 것이라 02편으로 옮겼습니다.

- 02편에 `CHECK_FATAL`·`WndProc` 가드·배율·Alt+Enter 전체 화면·mingw 대상 버전을 넣음. `tut02` 의 코드만 바뀌고 `tut03`·`tut04` 의 코드는 그대로
- 02편을 단계 커밋 7개로 다시 씀. 진입점과 메시지 루프를 2단계로 당겨 단계마다 실행되게 함. 순서 : 공통 도구 → 창과 루프 → WndProc → 끝나는 길 → 메시지 분배 → 창 크기 → 배율과 전체 화면
- 03편은 5단계. `MakeWindowAssociation` 은 스왑체인 단계로, `NotifyResize` 의 같은 크기 검사와 치명 실패 훅은 03편에 남김
- `tut01` 에 `wWinMain` 커밋을 더함. `UNICODE` 를 정의한 프로젝트라 진입점도 와이드 문자로 맞춤. 02편 2단계의 `Main.cpp` 는 본문 채우기만 남음
- main 이력을 한 줄로 다시 씀. `tut01` → 02편 단계 사슬 → 03편 단계 사슬 → 03편 시점 설정·문서 커밋 → 그 뒤 이력. 마지막 커밋의 트리가 옛 `tut03` 과 같아서 그 뒤 커밋은 충돌 없이 옮겨짐. 옛 이력은 `archive/before-rewrite-2026-09-25` 태그에 있음
- 단계 사슬의 첫 커밋(`0/7`)에 옛 tut02 시점의 설정 파일과 문서를 둠. 없으면 `.clang-format` 같은 설정이 tut01 것으로 남음

**칠 코드는 펼치고 CMake 는 소스를 모음 (2026-09-25)**

곁가지 분량 규칙(2026-09-23)이 따라 치기 원칙(2026-09-24)보다 먼저 생겨서 "따라 하기에 필요한 코드라도 접음" 이 남아 있었습니다. 코드를 모두 따라 치게 된 뒤로는 칠 코드를 접을 이유가 없었고, 점선 접기로 구분해도 매번 열어야 했습니다.

- 2절 곁가지 규칙을 "학습 블록 없음, 풀이와 개념 상자만 접음, 칠 코드는 펼침" 으로 바꿈. 7절의 `fold must` 는 없앰
- CMake 는 `file(GLOB_RECURSE ... CONFIGURE_DEPENDS Source/*.cpp)` 로 소스를 모음. 새 파일마다 치던 CMake 조각이 없어짐. VS 는 IDE 에서 파일을 넣으므로 그대로. 셰이더 복사도 `file(GLOB ...)` 으로 모음
- CMake 조각은 `CMake · 맥 빌드와 검사용, VS 만 쓰면 건너뜀` 접기로 옮김. 따라 하기의 기본 경로는 VS
- 이력을 01편 셋업 커밋부터 다시 써서 모든 태그의 `CMakeLists.txt` 가 바뀜. 코드와 프로젝트 파일은 그대로
- 04편을 7단계 커밋으로 다시 쌓음. 순서 : DirectXMath → RTV·뷰포트 → 버퍼 → 셰이더 컴파일 → 삼각형 → 상수 버퍼 → 카메라. 5단계는 행렬 없이 그려서 삼각형이 창을 채우고 창 비율을 따라 늘어나고, 6단계는 세 행렬을 항등으로 넘겨 5단계와 모양이 같음. "새 파일을 빌드에 등록한다" 단계는 없애고 단계마다 등록함. `external/DirectXMath` 는 1단계 커밋에 넣음
- 기각 : 상수 버퍼와 카메라를 한 단계로. 상수 버퍼·전치·투영이 한꺼번에 오고 중간에 확인할 화면이 없음
